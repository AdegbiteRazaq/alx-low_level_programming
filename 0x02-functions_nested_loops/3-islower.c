#include "main.h"
/**
*_islower - checks for lowercase character.
*@c: function paremeter.
*
*Return: 1 or 0.
*/
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (!);
	}
	else
	{
		return (0);
	}
}
